function solution(n) {
    let a = 1;
    let b = 2;

    for (let i = 3; i <= n; i++) {
        let temp = (a + b) % 1234567
        a = b;
        b = temp
    }

    return n === 1 ? a : b;
}

[코드 리뷰]
문제를 풀 수 있는 방법이 떠오르지 않아 정답을 확인해보니
해당 문제도 피보나치 수열을 이용해 정답을 풀 수 있었다.

예를 들어 1칸일 경우는 1가지 방법 2칸일 경우는 2가지 방법이 있다면
3칸일 경우는 (1칸 + 2칸)의 경우의 수 인 3이 되고 4칸은 (2칸 + 3칸)의 경우의 수인 5가 된다.

n을 작은 값부터 하나씩 종이에 적어봤으면 해당 규칙을 찾아볼 수 있었을텐데
그게 참 아쉽다...

결국 a와 b에는 1칸일 경우 2칸일 경우의 수를 적고
반복문은 3칸일 경우부터 시작한다.

temp에는 a와 b를 더한 값을 1234567로 나눈 나머지를 넣어주고
a에는 이전 b값을 b에는 새로 생긴 temp값을 넣어줘 이전 값들을 계속해서 더해준다.

n이 1이 아니라면 b값을 최종적으로 리턴해주면 된다.
