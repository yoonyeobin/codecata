이번 문제는 동일한 정수 배열의 내적을 리턴하는 문제이다
내적이란 말을 처음 듣는데 대충 아래와 같다고 한다.

a         b
[1,2,3,4]	[-3,-1,0,2]	

a와 b의 내적은 1*(-3) + 2*(-1) + 3*0 + 4*2 = 3 입니다.

같은 인덱스끼리 곱해서 그 총 합을 리턴하면 될 것 같다

function solution(a, b) {
    var answer = 0;
    for(let i =0; i < a.length; i++) {
        answer += a[i] * b[i];
    }
    return answer;
}

for문으로 a와 b의 같은 인덱스끼리 곱한 값을 계속 더하기할당 해주었다!
