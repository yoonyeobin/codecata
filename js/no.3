이번 문제는 num1을 num2로 나눈 몫을 결과값으로 추출하는 문제다
먼저 나누기 연산은 '/'로 사용하면 된다고 한다.
그런데.. 나머지가 아닌 몫만 결과로 추출해야한다..

구글링을 해본 결과 아래와 같은 함수를 알 수 있었다.
'Math.floor()' 해당 함수를 이용하면 소수점을 모두 버릴 수 있다고 한다.

그래서 내가 만든 코드는 아래와 같다.
function solution(num1, num2) {
    var answer = num1/num2;
    return Math.floor(answer);
}
