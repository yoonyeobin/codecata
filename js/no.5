이번 문제는 2개의 변수값이 같으면 1 다르면 -1로 출력되도록 하는 문제이다.
그럼 먼저 같다와 다르다가 자바스크립트에서는 어떤 기호를 사용하는지 확인해보았다.

같다 같은 경우에는'=='를 사용하고
다르다 같은 경우에는 '!='를 사용한다고 한다.

조건문을 이용하여 같을 경우에는 결과값이 1 다르면 -1이 되도록 코드를 짜보았다.

function solution(num1, num2) {
    var answer = 0;
    if(num1==num2) {
        answer=1
    } if(num1!=num2) {
        answer=-1
    }
    return answer;
}
