이번 문제는 0부터 9까지의 숫자가 임의로 들어있는 배열의 없는 값을 모두 찾아 더하여 출력하는 문제이다.
조건이 0부터 9까지이므로 for문의 i는 1로 초기화 하고 10까지 작업을 돌렸다.
for문 안에 if문을 넣어 includes 메서드를 이용하여 numbers안에 요소가 아닌 값들을 모두 더해 출력하였다

function solution(numbers) {
    var answer = 0
    for (let i = 1; i < 10; i++) {
        if (numbers.includes(i)) {
            answer += 0;
        } else {
            answer += i;
        }
    }
    return answer;
}
