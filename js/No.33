이번 문제는 길이가 n이고 수박수박수와 같은 패턴을 유지하는 문자열을 리턴하는 함수를 만드는 문제이다

이번 문제도 for문을 통해 손쉽게 해결하였다.

function solution(n) {
    var answer = '';
    for (let i = 1; i <= n; i++) {
        if(i % 2 !== 0)  {
            answer += "수";
        } else {
            answer += "박";
        }
    }
    return answer;
}

for문을 통해 i의 값이 홀수이면 수 짝수이면 박을 answer에 추가하여
answer을 최종적으로 리턴하였다
