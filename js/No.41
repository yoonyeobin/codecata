이번 문제는 두 수의 최대공약수와 최소공배수를 반환하는 함수를 만드는 것이다.

생각보다 오래 걸렸다...
최소공약수를 구하는 공식과 최대공배수를 구하는 공식을 만드는데
시간을 많이 허비한 것 같다...

결국 반복문을 통해 테스트는 통과하였다.

function solution(n, m) {
    var answer = [];
    // 최대공약수를 구하는 공식
    for(let i = n; i >= 1; i--) {
        if (n % i === 0 && m % i === 0){
            answer.push(i);
             break;
        }
    };
    let max = Math.max(n, m);
    // 최소공배수를 구하는 공식
    for(let i = max; i < Infinity; i++) {
        if (i % n === 0 && i % m === 0){
            answer.push(i);
            break;
        }
    }
    return answer;
}

코드를 설명하자면 첫번째 반복문을 통해 2개의 숫자중 아무 숫자나 i로 시작하여(큰 수나 작은 수나 결국 작은수까지 떨어지기 때문에 굳이 구분하지 않음)
1씩 감소하면서 나누어봤을 때 두개의 숫자가 모두 나머지가 0일때 바로 반복문을 종료시키고

반대로 2번째 반복문은 둘중 큰 값을 먼저 구하여 큰 값부터 시작해(구분하지 않으면 1일 경우 1에서 끝나버린다)
i를 각각의 수로 나누었을 때 나머지가 0일때 반복문을 종료시켰다!
